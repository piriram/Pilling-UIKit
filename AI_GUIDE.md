# AI Guide - Pilling UIKit

이 문서는 Claude, Gemini, Codex가 공통으로 따라야 하는 Pilling UIKit 프로젝트의 단일 AI 운영 규칙이다.

도구별 진입 파일:

- `AGENTS.md`: Codex용 진입점
- `CLAUDE.md`: Claude용 진입점
- `GEMINI.md`: Gemini용 진입점

위 파일들은 이 문서를 source of truth로 참조한다. 규칙을 수정할 때는 이 파일만 수정한다.

## 1. 작업 방식

- 긴 작업을 진행할 때 중간에 불필요한 확인을 요청하지 말고 끝까지 완료한다
- 여러 파일을 수정하는 작업도 한 번에 진행한다
- 사용자가 명시적으로 멈추거나 확인을 요구한 경우에는 그 지시를 우선한다
- 작업 전후로 `git status --short --branch`를 확인해 의도하지 않은 변경을 구분한다
- 사용자가 만든 변경을 임의로 되돌리지 않는다

## 2. 작업 종료 루틴

사용자 요청이 하나 끝나고 실제 산출물 변경이 있었다면 아래 순서를 기본으로 따른다.

1. `개발로그/YYYY-MM-DD.md`에 짧게라도 로그를 남긴다
2. 변경사항을 `git add` 한다
3. 규칙에 맞는 커밋 메시지로 `git commit` 한다
4. 마지막 답변에서는 무엇을 로그/커밋했는지 짧게 알려준다

단, 사용자가 명시적으로 커밋하지 말라고 했거나 변경이 미완성 기준점이면 커밋을 보류할 수 있다.

## 3. 개발로그 규칙

개발로그는 단순 작업 기록이 아니라 나중에 포트폴리오, 면접 답변, 회고에 재사용할 수 있는 경험 자산으로 작성한다.

최소 항목:

- 무엇을 만들었는지
- 왜 필요한 작업이었는지
- 어떤 액션을 했는지
- 다음 액션 또는 배운 점

작성 방식:

```text
결론(What / Product)
-> 배경(Why / 3C)
-> 실행(How / 4P)
-> 배운 점 / 다음 액션
```

파일 규칙:

- 파일명은 `개발로그/YYYY-MM-DD.md`
- 하루에 여러 번 써도 같은 파일에 누적
- 큰 전환점이 있으면 `## 세션 N`으로 나눈다
- 템플릿은 `개발로그/000_TEMPLATE.md`를 따른다

## 4. 문서 저장 규칙

기획, API, 설계, 운영 기준처럼 다음 작업의 기준점이 되는 내용은 문서로 남긴다.

- 프로젝트 루트에는 전체 프로젝트 기준 문서를 둔다
- 기능별 상세 문서는 명확한 대문자/한글 파일명으로 저장한다
- 예: `PILLING_SERVER_API_GUIDE.md`, `브랜치전략_Pilling.md`
- 법적 고지, 테스트 정책, 외부 공개용 문서는 필요 시 `docs/` 하위에 둔다
- 임시 메모나 서버 클론 같은 작업용 산출물은 repo에 넣지 않는다

문서는 단순 기록이 아니라 이후 구현, PR, 면접/포트폴리오 설명에 재사용 가능한 기준으로 작성한다.

## 5. Git 커밋 규칙

- 커밋 메시지에 이모지를 사용하지 않는다
- 제목은 간결하고 명확하게 작성한다
- 타입은 영어 Prefix를 사용한다
- 기본 형식은 `<Type>: <대상 + 핵심 변경 내용>`이다
- 대표 타입은 `Feat`, `Fix`, `Docs`, `Style`, `Chore`, `Merge`이다

예:

```text
Docs: 서버 API 연동 가이드 추가
Feat: 서버 알림 토큰 등록 흐름 추가
Fix: 복약 상태 매핑 오류 수정
```

아래 조건이면 가급적 바로 커밋한다.

- 문서가 추가/수정되었다
- 구조/설계 결정이 파일로 남았다
- 코드/스크립트가 추가되었다
- 다음 작업의 기준점이 생겼다

## 6. 브랜치 규칙

기능 작업은 먼저 GitHub Issue를 만들고 해당 번호로 브랜치를 생성한다.

- `feat/#<issue-number>`
- `fix/#<issue-number>`
- `docs/#<issue-number>`
- `chore/#<issue-number>`

예:

- `feat/#2`
- `fix/#8`
- `docs/#12`

주의:

- `#`가 들어간 브랜치 이름은 셸에서 주석처럼 해석될 수 있으므로 명령어에서는 반드시 따옴표를 사용한다
- PR 본문에는 가능하면 `Closes #<issue-number>`를 포함한다

## 7. 코드 스타일

- Swift 코드는 기존 프로젝트 스타일을 따른다
- 불필요한 주석을 추가하지 않는다
- 이모지는 코드나 커밋에 사용하지 않는다
- 비밀키, `.env`, `GoogleService-Info.plist`, 인증서, 프로비저닝 파일은 커밋하지 않는다

## 8. 예외

아래 경우는 로그 또는 커밋을 생략할 수 있다.

- 사용자가 명시적으로 커밋하지 말라고 한 경우
- 탐색만 했고 파일 변경이 전혀 없는 경우
- 변경이 너무 미완성이라 기준점으로 잡기 애매한 경우
- git hook 또는 저장소 정책이 현재 커밋을 차단하는 경우

단, 설계 결정이나 운영 기준이 생겼다면 로그는 남기는 쪽을 우선한다.
