# Claude Code 프로젝트 설정

## 작업 방식

- 긴 작업을 진행할 때 중간에 확인을 요청하지 말고 끝까지 완료할 것
- 여러 파일을 수정하는 작업도 한 번에 진행할 것

## Git 컨벤션

### 브랜치 전략

```
main     ← 안정 상태, 배포 기준 (태그로 배포 시점 기록)
develop  ← 평소 작업 기준 (feature 브랜치는 여기서 분기 → 여기로 머지)
```

### 브랜치 네이밍

```
타입/이슈번호-설명

feat/1-server-sync
fix/2-cycle-crash
refactor/3-pillstatus
```

- `#`은 shell에서 주석으로 인식될 수 있어 브랜치명에 쓰지 않음 (`feat/1-...`, `feat/#1-...` 금지)

### 커밋 메시지 형식

- 이모지 사용 금지
- 형식: `타입: 한국어 요약` (한 줄, 본문 없음 — 맥락은 PR 본문에)
- 타입(영어 고정): `feat / fix / refactor / docs / chore / test`
- 코드 식별자·기술 용어는 원문 유지 (번역 안 함)
- 구조 변경(refactor)과 동작 변경(feat/fix)을 같은 커밋에 섞지 않음
- 커밋 단위 기준: "이 커밋 하나만 revert해도 안전한가"

예시:
```
refactor: PillStatus 구조 개선 (TimeContext 도입)
fix: syncPillToServer race condition으로 PATCH 미전송 버그 수정
```

나쁜 예:
```
"작업 중"              → 무슨 작업인지 모름
fix: 수정               → 내용 없음
feat: 동기화 + UI 수정   → 두 목적 혼합, 분리 필요
```

### 커밋 전 루틴

```bash
git diff              # 뭘 바꿨는지 전체 확인
git add -p            # 청크 단위로 선택적 스테이징
git diff --staged     # 커밋에 들어갈 것만 확인
git commit
```

### PR 규칙

- 제목 형식 커밋과 동일: `타입: 요약`
- 본문에 변경 내용 / 테스트 확인 항목 / `Closes #N` 기재
- 가이드라인: diff 400줄 넘으면 분리 검토 (설정 파일 등 예외)

## 문서 작성 규칙

- 마크다운 파일은 간결하게 작성
- README나 문서는 꼭 필요한 경우가 아니면 생성하지 말 것
- 코드 주석과 간단한 설명 위주로 작성

## 코드 스타일

- Swift 코드는 기존 프로젝트 스타일 따르기
- 불필요한 주석 추가하지 말 것
- 이모지는 코드나 커밋에 사용하지 말 것
